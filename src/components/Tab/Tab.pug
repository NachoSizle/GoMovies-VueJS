.tab--container
  routerLink(
    :to='data.to'
  )
    svgicon(
      v-if='data.icon && data.icon !== ""'
      :icon="data.icon"
      width="1.5rem"
      height="1.5rem"
    )
    p(
      v-if='data.title && data.title !== ""'
    ) {{ data.title }}